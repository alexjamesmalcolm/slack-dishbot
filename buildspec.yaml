version: 0.2

phases:
  install:
    commands:
      - npm i
  build:
    commands:
      - npm test && npm run build
artifacts:
  files:
    - node_modules
    - package-lock.json
    - package.json
    - ./*
